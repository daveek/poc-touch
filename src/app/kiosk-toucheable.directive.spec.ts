import { KioskToucheableDirective } from './kiosk-toucheable.directive';

describe('KioskToucheableDirective', () => {
  it('should create an instance', () => {
    const directive = new KioskToucheableDirective();
    expect(directive).toBeTruthy();
  });
});
